on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $HOME
          id
          pwd
          echo '-------------------------------------------------------------------'
          find / -name "installdependencies.sh" || :
          cat /proc/self/uuid_map
